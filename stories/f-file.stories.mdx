import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks';
import fFile from '../src/component/f-file.vue'
import triggerComponent from './triggerComponent.vue'

<Meta title="f-file" component={fFile} />

## f-file

# 选单个文件

<Preview>
  <Story name="单个文件">
  {
    {
      data() {
        return {
          value: []
        }
      },
      components: { fFile },
      methods: {
        log(val) {
            console.log('input', val)
        }
      },
      template: `<fFile tip="只能选一个文件哦" v-model="value" @input="log" />`,
    }
  }
  </Story>
</Preview>

# 选多个文件

<Preview>
  <Story name="多个文件">
  {
    {
      data() {
        return {
          value: []
        }
      },
      components: { fFile },
      methods: {
        log(val) {
            console.log('input', val)
        }
      },
      template: `<fFile v-model="value" multiple @input="log" />`,
    }
  }
  </Story>
</Preview>

# drag

drag and triggerComponent

```javascript
// triggerComponent.vue
<template>
<div>
  <i class="el-icon-upload"></i>
  <div class="el-upload__text">将文件拖到此处，或<em>点击上传</em></div>
</div>
</template>
```

<Preview>
  <Story name="drag">
  {
    {
      data() {
        return {
          value: [],
          triggerComponent // import triggerComponent from './triggerComponent.vue'
        }
      },
      components: { fFile },
      methods: {
        log(val) {
            console.log('input', val)
        }
      },
      template: `<fFile drag :triggerComponent="triggerComponent" v-model="value" @input="log" />`,
    }
  }
  </Story>
</Preview>

# Props

<Props of={fFile} />